name: ${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}

services:
  db:
    image: postgres:16-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-rigging_interlock}
      POSTGRES_USER: ${POSTGRES_USER:-rigging_interlock}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rigging_interlock_dev}
    ports:
      - "${DB_PORT:-57528}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rigging_interlock} -d ${POSTGRES_DB:-rigging_interlock}"]
      interval: 5s
      timeout: 4s
      retries: 20
      start_period: 5s

  backend:
    build:
      context: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}-backend
    restart: unless-stopped
    environment:
      GIN_MODE: ${GIN_MODE:-release}
      PORT: "8080"
      DB_DRIVER: postgres
      DB_DSN: ${DB_DSN:-host=db user=rigging_interlock password=rigging_interlock_dev dbname=rigging_interlock port=5432 sslmode=disable TimeZone=Asia/Shanghai}
      DB_AUTO_MIGRATE: ${DB_AUTO_MIGRATE:-true}
      JWT_SECRET: ${JWT_SECRET:-change-this-stage-rigging-secret-at-least-32-bytes}
      JWT_TTL_MINUTES: ${JWT_TTL_MINUTES:-480}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:18528}
      TIMELINE_STEP_MS: ${TIMELINE_STEP_MS:-100}
      MAX_CUES_PER_RUN: ${MAX_CUES_PER_RUN:-40}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-240}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SHUTDOWN_TIMEOUT_SECONDS: ${SHUTDOWN_TIMEOUT_SECONDS:-10}
    ports:
      - "${BACKEND_PORT:-19528}:8080"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 4s
      retries: 20
      start_period: 5s

  frontend:
    build:
      context: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18528}:80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1/"]
      interval: 5s
      timeout: 4s
      retries: 20
      start_period: 5s

volumes:
  postgres_data:
    name: ${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}-postgres-data
